version: "2"
sql:
  - engine: "sqlite"
    queries: "internal/queries/"
    schema: "internal/migrations/schema"
    gen:
      go:
        package: "repository"
        out: "./internal/repository"

