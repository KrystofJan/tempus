-- Initialize current_entry table with a single record
INSERT INTO current_entry (id, current_entry_id) VALUES (1, NULL);

-- Initialize current_task table with a single record  
INSERT INTO current_task (id, current_task_id) VALUES (1, NULL);
